# remember: run bat cache --build to add the theme
--theme="active_theme"
# no grid
#--style="full,-grid"
--italic-text=always
# map ROFI syntax onto CSS as relatively close
--map-syntax "*.rasi:CSS"
